<h1 class="text-2xl font-bold mb-6">議案検索結果一覧</h1>

<% if @bills.any? %>
  <ul class="space-y-2">
    <% @bills.each do |bill| %>
      <li>
        <%= link_to bill.title, bill_path(bill), class: "text-blue-600 hover:underline" %>
      </li>
    <% end %>
  </ul>
<% else %>
  <p>該当する議案はありません。</p>
<% end %>
